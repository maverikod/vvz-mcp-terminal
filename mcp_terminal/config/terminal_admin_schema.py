"""Shared ``terminal.admin`` shape for generator and validator."""

from __future__ import annotations

from typing import Any, Dict

TERMINAL_ADMIN_CONFIG: Dict[str, Any] = {
    "allow_purge_sessions": True,
}
